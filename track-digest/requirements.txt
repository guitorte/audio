# track-digest — compact AI digest + batch time-crop for a music-to-midi track.
# Light stack only: consumes existing .mid + .wav, no separation/transcription deps.

# --- audio analysis + I/O ---
librosa>=0.10
soundfile>=0.12
numpy>=1.23
scipy>=1.10

# --- MIDI ---
pretty_midi>=0.2.10
mido>=1.3

# --- notebook widgets (Colab) ---
ipywidgets>=8.0
